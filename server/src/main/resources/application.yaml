ktor:
    application:
        modules:
            - com.matijasokol.notes.server.ApplicationKt.module
    deployment:
        port: 8888

# local database configuration for testing
database:
    connection: "jdbc:postgresql://localhost:5431/notes"
    username: "postgres"
    password: ""
    poolSize: 5